# R25 · 体验残留大扫除（编排直做）

Cloud Agent 配额不可用；用户要求加快节奏，本轮一次清掉 R20–R22 playfeel 续档里可安全落地的项：

1. 选轨 Esc 兜底日志分叉 + reduce 即时开关  
2. 闯城选定即 `FC.write` + reduce 关闭 + aria-live  
3. 主/二级合约 ARIA  
4. ≤360px 选择卡密度  

未动：玩法数值、原生 `<dialog>`、装饰层 effects vh。
